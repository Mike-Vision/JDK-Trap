<UserControl
    x:Class="JDKTrap.UI.Elements.Controls.OptionControl"
    x:Name="RootControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:JDKTrap.UI.Elements.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:JDKTrap.UI.ViewModels"
    xmlns:resources="clr-namespace:JDKTrap.Resources"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <local:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisibilityConverter"/>
    </UserControl.Resources>

    <ui:CardControl Margin="0,8,0,0">

        <ui:CardControl.Header>
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        x:Name="HeaderTextBlock"
                        Grid.Column="0"
                        FontSize="14" />

                    <TextBlock
                        x:Name="HelpLinkContainer"
                        Grid.Column="1"
                        Margin="4,0,0,0">
                        <Hyperlink
                            x:Name="HelpHyperlink"
                            Command="{x:Static models:GlobalViewModel.OpenWebpageCommand}"
                            TextDecorations="None"
                            ToolTip="{x:Static resources:Strings.Menu_MoreInfo}">
                            <ui:SymbolIcon Margin="0,1,0,0" Symbol="QuestionCircle48"/>
                        </Hyperlink>
                    </TextBlock>
                </Grid>

                <local:MarkdownTextBlock
                    x:Name="DescriptionTextBlock"
                    Margin="0,2,0,0"
                    FontSize="12"
                    Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </ui:CardControl.Header>

        <ui:CardControl.Content>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"/>

                <ContentPresenter
                    x:Name="InnerContentPresenter"
                    Grid.Column="1"
                    Margin="16,0,0,0"
                    VerticalAlignment="Center" />
            </Grid>
        </ui:CardControl.Content>

    </ui:CardControl>
</UserControl>